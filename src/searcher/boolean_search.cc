//
//  boolean_search.cc
//  search_engine
//
//  Created by Felipe Moraes on 4/16/15.
//
//

#include "boolean_search.h"


vector<unsigned> BoooleanSearch::intersection_terms(vector<Term>* &terms){
    vector<unsigned> result;
    //   set_intersection(v1.begin(), v1.end(),
    //                    v2.begin(), v2.end(),
    //                    back_inserter(result));
    return result;
}


vector<unsigned> BoooleanSearch::union_terms(vector<Term>* &terms){
    vector<unsigned> result;
    //   set_intersection(v1.begin(), v1.end(),
    //                    v2.begin(), v2.end(),
    //                    back_inserter(result));
    return result;
}
